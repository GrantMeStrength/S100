# Rust build artifacts
/core/target/
/core/pkg/

# wasm-pack generated files — WASM binary and JS bindings are committed
# so the app works without needing a local Rust/wasm-pack toolchain.
# Run `npm run build:wasm` after any changes to core/src/


# Node / Vite
/web/node_modules/
/web/dist/

# macOS
.DS_Store

# Root-level disk image collection (working copies).
# Disk images to be served by the app live in web/public/disks/ instead.
/disks/
